# Wafer Map Optimizer 软件技术文档

## 1. 概述

晶圆芯片布局优化与导出工具。给定晶圆直径、边缘排除量、芯片尺寸与 Notch 朝向，通过两种布局模式计算晶圆内可容纳的最大芯片排布，可视化展示，并支持导出图片、SINF3D 与 GDS/OAS 布局文件。

- 输入：晶圆几何参数、芯片尺寸、Notch 朝向、偏移/寻优设置
- 输出：行列数、总芯片数、偏移量、前 8 颗芯片距边距离、可视化晶圆图、导出文件

## 2. 技术栈

| 项目 | 说明 |
|------|------|
| 语言 | Python 3 |
| GUI | PySide6（QMainWindow） |
| 数值计算 | NumPy |
| 图形绘制 | Matplotlib（FigureCanvasQTAgg 嵌入） |
| 布局文件 | gdstk（GDSII / OASIS 读写、布尔运算） |
| 打包 | PyInstaller |

## 3. 系统架构

单模块架构，核心类 `FinalWaferAnalyzer`（QMainWindow）位于 `Wafer_Map_Optimizer.py`。界面分为：

1. **左侧控制面板**（固定宽度 320px）
   - Wafer Parameters：晶圆与芯片几何参数
   - Centering：Fix Value / Auto 双 Tab
   - Generate Map 按钮
   - Analysis Results：统计量显示
   - 功能按钮组
2. **右侧画布**：Matplotlib 晶圆图

数据流：参数录入 → 参数校验 → 布局生成（固定偏移或寻优）→ 结果展示与绘图 → 导出。

## 4. 参数说明

| 参数 | 字段名 | 默认值 | 说明 |
|------|--------|--------|------|
| Wafer Diameter (mm) | `wafer_dia` | 300 | 晶圆直径 |
| Edge Exclusion (mm) | `edge_excl` | 3 | 边缘排除量，有效半径 = 半径 − 此值 |
| Chip X (mm) | `chip_x` | 32 | 芯片 X 方向尺寸 |
| Chip Y (mm) | `chip_y` | 45 | 芯片 Y 方向尺寸 |
| Orientation | `notch_angle` | 180 | Notch 朝向，0 / 90 / 180 / 270 |
| Offset X / Y (mm) | `offset_x` / `offset_y` | 0 | Fix Value 模式固定偏移 |
| Optimize Step (1/) | `optimize_step` | 4 | Auto 模式寻优步长分母 |

## 5. 核心算法

### 5.1 参数校验（get_parameters）

读取并转换参数，校验均为正数且 Edge Exclusion 小于晶圆半径；非法输入抛出明确错误。

### 5.2 布局模式

- **Fix Value**：直接以 Offset X/Y 为原点调用 `generate_tile_layout`。
- **Auto**：`find_optimal_offset` 枚举基础偏移 `(0,0)`、`(chip_x/2,0)`、`(0,chip_y/2)`、`(chip_x/2,chip_y/2)` 与自适应网格偏移（按 `optimize_step` 等分芯片尺寸），取芯片数最多的偏移。

### 5.3 布局生成（generate_tile_layout）

以偏移点为原点，环状扩张（步长 = 芯片尺寸）遍历 `dx`、`dy` 网格：

1. 逐环枚举候选位置，`is_valid_position` 校验有效性。
2. 有效则记录坐标到 `(col, row)` 映射。
3. 一整环无新增有效位置时终止。

### 5.4 有效性判定（is_valid_position）

芯片四角均落在有效圆内（半径 `eff_radius = 晶圆半径 − 边缘排除量`）时有效：

```
四角距离² ≤ eff_radius²
```

### 5.5 距边距离（compute_die_margin / nearest_dies）

每颗芯片取四角到有效边缘余量的最小值作为贴边距离，排序后取前 8 颗。

## 6. 可视化

- 晶圆外轮廓、有效区虚线圆、Notch、中心十字参考线、die 网格与坐标标注。
- die 轮廓颜色区分模式：Fix Value 深蓝（`#1F4E79`）、Auto 绿色（`#2E7D32`）。
- 交互：左键拖拽框选放大、右键拖拽框选缩小；悬停右上角显示 `X/Y` 坐标。
- 「Min Dist」标签可点击，切换对应芯片的距边标注显示。

## 7. 导出格式

### 7.1 SINF3D

文本格式布局，含 `FNLOC`（Notch 朝向）、`ROWCT`、`COLCT`、`XDIES`、`YDIES` 与逐行 die 网格（`000` 有效 / `___` 空位），行数据倒序输出。

### 7.2 GDS / OAS

`Library(unit=1e-3 m, precision=1e-9 m)`，结构：

| 内容 | Layer | 规格 |
|------|-------|------|
| Chip 单元 | 101 | 单颗芯片矩形，被 TOP 引用 |
| Wafer 轮廓 | 200 | 360 点多边形，切除 SEMI-M1 标准 Notch |
| EE 轮廓 | 201 | 360 点多边形 |

**Notch 几何**（SEMI-M1 标准）：V 型缺口、夹角 90°（两侧相对径向各 45°）、径向切深 1.0 mm、槽底圆角 R=0.2 mm。实现上：

1. `regular_polygon` 生成 360 点晶圆正多边形。
2. 以槽底最深点、槽壁方向、圆角圆心建立局部坐标系，构造缺口多边形（`b_pt`/`c_pt` 开口端、`t1`/`t2` 圆角切点、13 点底部圆弧）。
3. 用 `gdstk.boolean(wafer, notch, 'not')` 差集切除缺口。
4. 输出前删除 `t1`/`t2` 两个槽壁切点（容差 1e-3 mm，覆盖布尔运算 1e-3 mm 量化误差），使轮廓仅保留圆角圆弧（共 375 点）。

## 8. 打包部署

PyInstaller 配合 `Wafer Map Optimizer.spec` 打包为单文件可执行程序（无控制台窗口）。图标与资源通过 `resource_path` + `sys._MEIPASS` 在打包/开发模式下定位。
