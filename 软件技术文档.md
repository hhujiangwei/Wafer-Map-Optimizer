# Wafer Map Optimizer 软件技术文档

## 1. 概述

Wafer Map Optimizer 是一款晶圆芯片布局优化工具。给定晶圆直径、边缘排除量以及芯片（die）的 X/Y 尺寸，通过遍历偏移量进行寻优，计算晶圆内可容纳的最大芯片数量及其排布，并以图形方式展示布局，同时给出距离有效边缘最近的前四颗芯片的边缘余量。

- 输入：晶圆几何参数与芯片尺寸

- 输出：最大芯片数、行列数、最优偏移量、前四颗芯片贴边距离、可视化晶圆图、SINF 导出文件

## 2. 技术栈

| 项目   | 说明                                  |
| ---- | ----------------------------------- |
| 语言   | Python 3                            |
| GUI  | Tkinter / ttk                       |
| 数值计算 | NumPy                               |
| 图形绘制 | Matplotlib（FigureCanvasTkAgg 嵌入 Tk） |
| 打包   | PyInstaller                         |

## 3. 系统架构

软件为单模块架构，全部功能封装于类 `FinalWaferAnalyzer` 中，位于主脚本 `WaferMapOptimizer.py`。整体分为四个部分：

1. **参数输入**（左方面板）：晶圆参数录入
2. **操作控制**：生成地图、导出 SINF 两个按钮
3. **结果展示**：统计量只读显示
4. **可视化**：右侧 Matplotlib 画布绘制晶圆图

数据流：参数录入 → 参数校验 → 偏移寻优 → 布局生成 → 结果展示与绘图 →（可选）SINF 导出。

## 4. 参数说明

| 参数                  | 字段名             | 默认值 | 说明                             |
| ------------------- | --------------- | --- | ------------------------------ |
| Wafer Diameter (mm) | `wafer_dia`     | 300 | 晶圆直径                           |
| Edge Exclusion (mm) | `edge_excl`     | 3   | 边缘排除量，有效半径 = 半径 − 此值           |
| Chip X (mm)         | `chip_x`        | 32  | 芯片 X 方向尺寸                      |
| Chip Y (mm)         | `chip_y`        | 45  | 芯片 Y 方向尺寸                      |
| Orientation         | `notch_angle`   | 180 | Notch 朝向，可选 0 / 90 / 180 / 270 |
| Optimize Step (1/)  | `optimize_step` | 4   | 寻优步长分母，可选 1 / 2 / 4 / 5 / 10   |

## 5. 核心算法

### 5.1 参数校验（get\_parameters）

读取并转换为数值类型，校验所有参数为正，且边缘排除量不得超过晶圆半径。

### 5.2 偏移寻优（find\_optimal\_offset）

- 候选偏移由两部分组成：

  - 4 个基础偏移：`(0,0)`、`(chip_x/2,0)`、`(0,chip_y/2)`、`(chip_x/2,chip_y/2)`

  - 自适应偏移：将芯片尺寸按 `optimize_step` 等分，网格枚举全部偏移组合

- 对每个候选偏移生成布局，比较芯片数。

- 数量相同且候选为基础偏移时，优先选取基础偏移作为最优结果。

### 5.3 布局生成（generate\_tile\_layout）

以 die 网格为坐标系，采用**环状扩张**策略：

1. 从原点 `(0,0)` 出发，逐环扩展 `dx`、`dy`（步长为单颗芯片尺寸）。
2. 每个候选位置调用 `is_valid_position` 判定有效性。
3. 有效则加入布局，记录 `(x, y) → (col, row)` 映射（col 对应 dx，row 对应 dy）。
4. 当一整环均无新增有效 die 时终止。

### 5.4 有效性判定（is\_valid\_position）

芯片中心为 `(x, y)`，四角坐标为 `(x±chip_x/2, y±chip_y/2)`。当四角均落在有效圆（半径 `eff_radius = 晶圆半径 − 边缘排除量`）内时判定为有效，即满足：

```
角距离² ≤ eff_radius²
```

### 5.5 前四颗芯片统计（get\_corner\_distances）

1. 对每颗有效芯片，计算其四角到有效边缘的余量（`eff_radius − 角到圆心距离`）。
2. 取该芯片四角余量的**最小值**作为该芯片的贴边距离。
3. 对所有芯片的贴边距离排序，取前 4 个即为距离边缘最近的四颗芯片。

## 6. 用户界面

- **Wafer Parameters**：芯片参数录入区

- **Control Panel**：`Generate Map`（执行寻优与绘图）、`Export SINF`（导出布局文件）

- **Analysis Results**：显示列数（Column Count）、行数（Row Count）、总芯片数（Total Dies）、最优偏移（Optimal Offset）、前四颗芯片贴边距离（Min Dist 1–4）

- **可视化画布**：绘制晶圆外轮廓、有效区虚线圆、Notch、中心十字参考线、die 网格与坐标标注

## 7. SINF 导出格式

`Export SINF` 将当前布局写入文本文件，结构如下：

```
DEVICE:
LOT:
WAFER:
FNLOC:<notch 角度>
ROWCT:<行数>
COLCT:<列数>
BCEQU:000
REFPX:0
REFPY:0
DUTMS:mm
XDIES:<chip_x>
YDIES:<chip_y>
RowData:...（每行 die 网格，000 表示有效 die，___ 表示空位）
```

行数据以倒序输出，以匹配标准地图朝向。

## 8. 打包部署

使用 PyInstaller 配合 `Wafer Map Optimizer.spec` 打包为单个可执行文件（无控制台窗口）。

```powershell
pyinstaller "Wafer Map Optimizer.spec"
```

- 图标：`temp_icon.ico`

- 资源打包：图标通过 `sys._MEIPASS` 在运行时定位（见 `resource_path`）

## 9. 已知限制与改进方向

- 布局合法性基于 die 四角均在有效圆内的判定，未考虑晶圆缺口（Notch）对有效区域的裁剪。

- 环状扩张依赖"有效区域连通凸"的假设，极端参数下理论存在边界情况。

- 偏移寻优中的基础偏移优先判定依赖浮点比较，存在极小精度误差影响平局时结果的可能。

- 空布局（无任何有效 die）下结果展示会抛异常，建议后续增加兜底提示。
